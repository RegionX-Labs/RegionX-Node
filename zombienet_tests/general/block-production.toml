[settings]
timeout = 1000

[relaychain]
chain = "westend-local" # TODO: Kusama local
default_args = ["-lparachain=debug,xcm=trace"]
command = "polkadot"

  [[relaychain.nodes]]
  name = "alice"
  balance = 2000000000000

  [[relaychain.nodes]]
  name = "bob"
  balance = 2000000000000

[[parachains]]
id = 2000
chain = "regionx-westend-dev"
addToGenesis = false

  [parachains.collator]
  name = "regionx-collator01"
  command = "regionx-node"
  args = [ "-lruntime=debug,parachain=trace" ]
